# Node
node_modules/
.pnpm-store/

# Next.js
apps/web/.next/
apps/web/out/

# Environment — never commit secrets
apps/web/.env.local
.env.local
.env

# Database — runtime data, not source
data/

# Compositor build artifacts
compositor/srt_compositor
compositor/srt_compositor.o
*.o

# Root-level duplicates (canonical source is compositor/)
/srt_compositor
/srt_compositor.c
/srt_compositor.o
/stream.sh
/Makefile
works/

# Uploads — user data (root uploads dir only, not app/api/uploads route)
/uploads/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Background video (user-supplied, not tracked)
background.mp4

# Local docker-compose overrides
docker-compose.override.yml
